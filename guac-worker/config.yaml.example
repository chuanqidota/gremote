s3:
  endpoint: "minio:9000"
  access_key: "your-access-key"
  secret_key: "your-secret-key"
  bucket: "gremote"
  use_ssl: false

# 每步转换超时（秒），guacenc 和 ffmpeg 各自的超时时间，默认600（10分钟）
convert_timeout: 600
